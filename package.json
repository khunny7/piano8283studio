{
  "name": "personal-portfolio-blog",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sitemap": "tsx scripts/generateSitemap.ts",
    "deploy": "npm run build && npx --package firebase-tools firebase deploy --only hosting",
    "deploy:preview": "npm run build && npx --package firebase-tools firebase hosting:channel:deploy preview",
    "deploy:production": "npm run build && npx --package firebase-tools firebase deploy --only hosting"
  },
  "dependencies": {
    "firebase": "^12.2.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.25.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.3",
    "@vitejs/plugin-react": "^4.3.1",
    "tsx": "^4.16.0",
    "typescript": "^5.4.0",
    "vite": "^5.3.0"
  }
}
